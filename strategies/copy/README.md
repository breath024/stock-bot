# 카피 트레이딩 (단계 5에서 구현)

'다른 분의 실거래/시그널을 받아 그대로 따라 매매'하는 전략이 여기 들어간다.

예정 구성:
- `signal_feed.py` — 텔레그램/웹훅/CSV로 들어온 외부 시그널 수신
- `follower.py`    — 받은 시그널을 Signal 객체로 변환해 봇에 주입

규칙형(rule_based)과 동일하게 `Strategy`를 상속하고 같은 `Signal`을 내보내므로,
엔진·브로커·백테스트 코드는 한 줄도 바꿀 필요가 없다.
