# 자동매매 봇 — 인수인계 (HANDOFF)

> 최종 갱신: 2026-05-31
> 작업 경로: `C:\Users\USER\Desktop\창업\주식봇`
> 상세 구조/실행법은 [README.md](README.md) 참고.

---

## 1. 프로젝트 한 줄 요약
다른 투자자들의 매매법을 **가상매매(백테스트 + 실시간 모의투자)**로 검증하는 한국 주식 자동매매 봇. Python.

## 2. 확정된 요구사항 (2026-05-31)
- 시장: **한국 주식** (KIS 한국투자증권 Open API 예정)
- 전략 입력: **① 규칙 직접 코딩 + ② 카피 트레이딩(남의 시그널 따라하기)** 둘 다
- 실행: **백테스트 + 실시간 모의투자** 둘 다
- 언어: **Python** (단계 1은 표준 라이브러리만으로 동작)

## 3. 핵심 설계 원칙
> **"전략은 Signal만 내고, 체결은 Broker가 한다."**
> 데이터피드 + 브로커만 교체하면 같은 전략이 백테스트 ↔ 모의투자에서 그대로 돈다.
> `[데이터피드] → [전략] → Signal → [브로커] → Fill → [계좌] → [리포트]`

## 4. 현재 진행 상태
- ✅ **단계 1 완료** — core + 모의체결 + 백테스트 동작 검증됨.
  `python run_backtest.py` → 합성데이터 250일 골든크로스 백테스트 (+15.34%, MDD -11.08%, 샤프 1.66) + 매매내역 CSV 저장.
- 로드맵:
  - [x] 1. core + 모의체결 + 백테스트 동작 ← **현재**
  - [ ] 2. 전략 다양화 + 리포트 고도화
  - [ ] 3. KIS/pykrx 과거데이터 (실데이터 백테스트) — **신청 불필요(pykrx 무료)**
  - [ ] 4. KIS 실시간 시세 + 실시간 모의투자 (`run_paper.py`) — **KIS 키 필요**
  - [ ] 5. 카피 시그널 수신(텔레그램/웹훅) → 팔로워
  - [ ] 6. 리스크 한도 + KIS 공식 모의투자 계좌 연동

---

## 5. ⭐ 의뢰자에게 받아야 할 정보 (다음 진행의 전제)

다음 설계(특히 단계 4 실시간)가 이 답변에 따라 갈립니다.

### A. 따라 할 매매법의 "속도" (가장 중요)
- [ ] **일봉/며칠 보유(스윙)?** → 지연 무관, 지금 구조 그대로 OK
- [ ] **분봉 단타(당일 몇 번)?** → 웹소켓만 붙이면 OK
- [ ] **초 단위 스캘핑/호가 잡기?** → 리테일 자동매매 한계 있음. 구조 재설계 필요
- 이유: 매매 속도에 따라 데이터(일봉 vs 틱)·주문 방식·실시간 아키텍처가 달라짐.

### B. 매매법 자체
- [ ] 누구의 매매법인가? 몇 명분인가?
- [ ] 규칙이 **명문화 가능한가?** (예: "5일선이 20일선 돌파 시 매수")
      → 규칙형(rule_based)으로 코딩 가능
- [ ] 아니면 **그 사람의 실거래/시그널을 받아 따라하는가?**
      → 카피형(copy). 시그널을 어떻게 받나? (텔레그램 채널 / 웹훅 / 수기 입력 / 종목추천방 등)

### C. 매매 범위·자금
- [ ] 대상 종목 유니버스 (특정 종목 몇 개 / 코스피200 / 테마 등)
- [ ] 초기 투자금 (모의 기준)
- [ ] 종목당 최대 비중, 손절 기준, 일일 최대손실 한도 등 리스크 규칙

### D. 실거래/계정
- [ ] KIS(한국투자증권) 계좌·Open API 신청 의향 있는지
- [ ] 최종 목표가 "모의투자까지"인지 "실계좌 자동매매"까지인지

---

## 6. 데이터/지연 관련 결정사항 (참고)
- **백테스트**: `pykrx`(무료, 신청 0)로 실제 종목 일봉 받아 검증 가능. 라이브엔 못 씀(지연됨).
- **실시간**: KIS 웹소켓이 시세를 push → 폴링 아님 → 실제 지연 보통 0.x초.
  일봉/분봉 전략엔 이 지연이 무의미. 초단타만 문제됨.

## 7. 다음에 할 일 (의뢰자 답변 오면)
1. 답변 받은 매매법을 `strategies/`에 구현 (규칙형이면 `_template.py` 복사)
2. (신청 없이) `pykrx` 붙여 실제 종목 백테스트 = 단계 3
3. 검증되면 KIS 신청 안내 후 단계 4(실시간 모의투자)

## 8. 주의
- 수수료·세율(`broker/simulated.py`)은 기본값. 실제 증권사/연도 기준으로 교체할 것.
- API 키는 `.env`에만, 절대 커밋 금지 (`.env.example` 참고).
- 콘솔 한글 깨지면 `$env:PYTHONUTF8=1; python run_backtest.py`.
